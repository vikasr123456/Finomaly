# Complete transitive closure resolved from the environment where
# `python -m unittest discover -s . -p "test_*.py"` passed (98 tests) on CPython 3.12.12.
# Install with: python -m pip install -r requirements.lock.txt
# Regenerate after any dependency change and re-run the offline tests.
#
# scikit-learn, scipy, joblib, cloudpickle, and threadpoolctl were removed:
# the Isolation Forest now ships in app/fraud_demo.py on numpy alone.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
et-xmlfile==2.0.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
narwhals==2.26.0
numpy==2.5.3
openpyxl==3.1.5
pydantic==2.13.5
pydantic-core==2.46.5
python-multipart==0.0.20
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.52.4
# exceptiongroup is only required by anyio on Python < 3.11; not needed here.
